<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- using a style sheet helps code to be efficient, instead of coping and pasting-->
   <!-- we can use just one line of code -->
    <style name="buttonRow">
        <item name="android:layout_weight">1</item>
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">0dp</item>
        <!-- Creating the rows for the number pad-->
    </style>

    <style name="buttonNumber">
        <item name="android:layout_weight">1</item>
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_height">match_parent</item>
        <item name="android:background">@null</item>
        <item name="android:textSize">25sp</item>
        <item name="android:onClick">numberAction</item>
    </style>
        <!-- Creating the numbers for the number pad-->

    <style name="buttonOperator">
        <item name="android:layout_weight">1</item>
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_height">match_parent</item>
        <item name="android:background">@null</item>
        <item name="android:textSize">25sp</item>
        <item name="android:textColor">@color/blue</item>
        <item name="android:onClick">operationAction</item>
        <!-- Creating the numbers for the number pad-->
    </style>
</resources>